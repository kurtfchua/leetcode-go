func twoSum(nums []int, target int) []int {
    hashmap := make(map[int]int)

    for i, v := range nums{
        if _, ok := hashmap[target-v]; ok{
            return []int{hashmap[target-v], i}
        }
        hashmap[v] = i
    }

    return []int{}
    
}