func maxProfit(prices []int) int {
    l, r := 0, 1
    max_profit := 0 

    for r < len(prices){
        if prices[r]-prices[l] > 0 {
            max_profit = max(max_profit, prices[r]-prices[l])
        }else{
            l = r
        }
        r++ 
    }
    
    return max_profit
}